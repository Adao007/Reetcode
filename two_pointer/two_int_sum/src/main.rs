struct Solution; 
impl Solution {
    fn get_two_sum(nums: &Vec<i32>, target: &i32) -> Vec<i32> {
        
    }
}

fn main() {
    let numbers = vec![1, 2, 3, 4];
    let target = 3; 

    assert_eq!(Solution::get_two_sum(&numbers, &target), target);
}
