// What are the steps for this problem? 
// 1. Given an array, return the top k frequent elements 
use std::collections::HashMap;

struct Solution; 
impl Solution {
    fn get_top_k(nums: Vec<i32>, k: i32) -> Vec<i32> {
        let mut frequency_map: HashMap<i32, Vec<i32>> = HashMap::new();

        todo!() // Temporary return to satisfy compiler
    }
}

fn main() {
    let nums = vec![1, 2, 2, 2, 3, 3, 3];
}
