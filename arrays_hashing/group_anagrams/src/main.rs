use std::collections::HashMap;

struct GroupAnagrams; 
impl GroupAnagrams {
    fn group_words (strs: Vec<String>) -> Vec<Vec<String>> {
        let mut map: HashMap<Vec<i32>, String>> = HashMap::new(); 

        for word in strs {
            let mut array = [0; 26];
        } 
    }
}

fn main() {
    let str1 = vec!("act", "pots", "tops", "cat", "stop", "hat");
    let str2 = vec!("x");
    let str3 = vec!(""); 
}
